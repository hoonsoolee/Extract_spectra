# CanopySpectra — 한글 실전 빠른 사용법

이 문서는 처음 프로그램을 사용하는 연구자가 긴 설명서를 모두 읽지 않고도
실제 분석을 한 번 수행할 수 있도록 만든 따라 하기 안내서입니다.

한국어 프로그램 실행:

```powershell
cd D:\Research\Hyperspectral\Extract_Spectra_v2
python -m streamlit run app.py
```

브라우저는 일반적으로 `http://localhost:8501`에서 자동으로 열립니다.

## 시작 전 확인

- ENVI 파일은 헤더와 바이너리를 같은 폴더에 둡니다. 예:
  `plot_01.bil.hdr` + `plot_01.bil`.
- 첫 확인은 공간 다운샘플링 `4`로 시작합니다. 최종 분석에서 메모리와 시간이
  충분할 때 `1`을 사용합니다.
- Raw DN은 반사율이 아닙니다. 논문용 스펙트럼은 호환되는 `QC PASS`
  보정파일을 적용해야 합니다.
- 전체 폴더를 돌리기 전에 단일 파일 하나로 설정을 점검합니다.

## 1. CERES 파일 하나를 준비하고 분석하기

예시 원본:

```text
D:\FieldData\2026-08-21\Plot_001.ceres
```

왼쪽 사이드바에서 다음 순서로 누릅니다.

```text
로컬 폴더
  -> D:\FieldData\2026-08-21 선택
  -> 단일 파일 선택
  -> 폴더 스캔
  -> Plot_001.ceres 선택
  -> CERES 내부 목록 읽기
  -> 촬영 구간/센서 선택 (예: A / VNIR)
  -> 빠른 미리보기
  -> 선택 항목 분석 준비
```

각 단계에서 하는 일:

1. **CERES 내부 목록 읽기**는 영상 전체가 아니라 레코드 헤더만 읽어 작은
   인덱스를 만듭니다.
2. **빠른 미리보기**는 RGB 또는 가색에 필요한 3개 밴드만 읽습니다.
3. **선택 항목 분석 준비**는 선택한 촬영 구간/센서만
   `output/_ceres_cache/` 아래의 `uint16` BIL/HDR로 변환합니다.
4. `현재 선택 항목이 일반 BIL/HDR 분석 입력으로 연결됨`이라는 초록색
   메시지를 확인합니다.
5. 다운샘플링, 클러스터링, 보정 및 리포트 설정을 고른 뒤 **분석 시작**을
   누릅니다.

중요: 현재 버전은 CERES 내부의 **선택한 항목 하나씩** 안전하게 준비하는
방식입니다. 여러 CERES 파일의 모든 내부 항목을 한 번에 자동 추출하는 기능은
아직 구현 전입니다. 전체 배치 분석은 미리 준비된 BIL/HDR을 대상으로 실행합니다.

## 2. BIL 또는 다른 초분광 파일의 스펙트럼 확인하기

### ENVI 파일 예시

```text
D:\FieldData\Plot_001.bil
D:\FieldData\Plot_001.bil.hdr
```

두 파일을 같은 폴더에 두고, 폴더 스캔 화면에서는 작은 `.hdr` 파일을
선택하는 것이 가장 안전합니다.

### ROI 스펙트럼 확인 순서

1. 메인 화면에서 **ROI 스펙트럼** 탭을 엽니다.
2. 초분광 파일을 선택합니다. 목록에 없으면 **직접 경로 입력** 또는
   **선택** 버튼을 사용합니다.
3. 처음에는 `다운샘플 ×4`를 선택하고 **로드**를 누릅니다.
4. 보정 상태 배너를 읽습니다.
   - **반사율 보정 적용됨**: 반사율 스펙트럼을 볼 수 있습니다.
   - **원본 DN**: 센서 카운트이며 절대 반사율이 아닙니다.
5. 잎이나 플랏이 작으면 먼저 **확대**합니다.
6. Box, Lasso 또는 클릭 Polygon으로 ROI를 지정합니다.
7. 평균, 중앙값, 표준편차와 파장 범위를 확인합니다.
8. 보정이 연결되어 있으면 **보정 반사율 / 원본 DN / 원본·보정 비교**를
   전환하여 무엇이 달라졌는지 확인합니다.
9. 저장 경로를 정하고 **CSV 저장**을 누릅니다. 보정이 적용된 경우 원본 DN
   비교 CSV도 함께 저장됩니다.

예시 해석:

```text
건강한 녹색 잎은 보통 660 nm 부근의 red 반사율이 낮고 NIR로 가면서
반사율이 크게 증가합니다. 특정 파장에서 갑자기 계단처럼 뛰거나 1보다
지나치게 큰 값이 나타나면 스펙트럼을 평활화하기 전에 보정파일을 점검합니다.
```

## 3. 클러스터링을 제대로 설정하고 확인하기

### 첫 시험 권장 설정

```text
처리 모드: 단일 파일 선택
공간 다운샘플링: 4
분류 방법: K-Means
클러스터 수: 6
리포트: 빠른 필드 QC
```

K-Means는 VNIR과 SWIR 모두에서 사용할 수 있어 첫 확인용으로 편합니다.
기본 설정은 원본 DN의 스펙트럼 구조로 클러스터를 만들고, 같은 마스크를
보정 데이터에 적용해 반사율 스펙트럼을 추출합니다. Hybrid는 NDVI와 밝기
임계값을 사용하므로 유효한 보정이 있으면 반사율을 입력으로 사용합니다.

분석 완료 후 **클러스터링 결과 이미지 검수**에서 다음을 비교합니다.

- RGB 원본 이미지
- 클러스터 컬러맵
- 투명도를 조절할 수 있는 RGB/클러스터 오버레이
- 클러스터 경계선
- 클러스터별 단독 이미지

좋은 결과는 잎, 토양, 깊은 그림자, 강하게 반사된 부분이 공간적으로 납득할
수 있게 나뉩니다. 클러스터 번호 자체에는 생물학적 의미가 없으므로 위치와
스펙트럼을 함께 보고 해석합니다.

| 화면에서 보이는 문제 | 먼저 바꿔볼 설정 |
|---|---|
| 잎 하나가 작은 조각으로 너무 많이 나뉨 | 클러스터 수를 줄임 |
| 밝은 잎과 그림자 잎이 모두 합쳐짐 | 클러스터 수를 늘리거나 Hybrid의 밝기값 조절 |
| VNIR에서 토양과 식생이 합쳐짐 | Hybrid를 사용하고 NDVI 임계값 점검 |
| SWIR에 red/NIR 파장이 없음 | Hybrid 대신 K-Means 또는 SAM 사용 |
| 전체는 괜찮고 특정 플랏만 이상함 | ROI 구역 분석·재클러스터링에서 그 ROI만 다시 시험 |
| 과반사 또는 패널 픽셀이 포화됨 | 촬영/마스크를 수정함. 클러스터 수로 소실 신호를 복원할 수 없음 |

색상이 보기 좋다는 이유만으로 결과를 채택하지 말고, 클러스터의 위치·면적,
스펙트럼, 경계와 유사 플랏에서의 반복성을 확인합니다.

## 4. White/Dark 반사율 보정파일 만들고 적용하기

이 프로그램에서 White balance는 RGB 색감 조정이 아니라 파장별 empirical-line
반사율 보정을 의미합니다.

```text
Reflectance = a(파장) × DN + b(파장)
```

### 준비할 자료

- 작물 촬영시간과 가까운 reference panel 영상
- 패널 인증 반사율: 예 `0.99`, `0.50`, `0.25`
- 렌즈 덮개 또는 셔터를 닫고 측정한 Sensor Dark
- 작물과 같은 센서, 밴드, integration time 및 gain

### 보정파일 생성 순서

1. **패널 보정** 탭을 엽니다.
2. 패널/reference 영상을 선택하고 `다운샘플 ×4`에서 **로드**합니다.
3. 패널의 깨끗하고 균일한 안쪽을 드래그합니다. 가장자리, 그림자, 먼지,
   glare는 피합니다.
4. 인증값을 소수로 입력합니다: `99%=0.99`, `50%=0.50`.
5. **이 영역을 패널로 추가**를 누르고 다른 패널도 반복합니다.
6. **실측 Dark 파일**을 선택하고 같은 조건의 Dark를 **Dark 로드**합니다.
   실측 Dark가 없을 때의 상수 `100 DN`은 빠른 점검용입니다.
7. **자동 반사율 보정 계산**을 누릅니다.
8. 패널 복원 스펙트럼, 파장별 사용 가중치, 포화/무효 밴드와 QC 등급을
   확인합니다.

50% 패널은 입력값 `0.50` 그대로 사용하며 99%로 바꾸지 않습니다. 99% 패널이
일부 파장에서만 포화되면 그 파장에서는 가중치가 0이 되고, 포화되지 않은
낮은 반사율 패널이 해당 구간을 담당할 수 있습니다.

### QC와 자동 적용

| QC 결과 | 의미와 사용법 |
|---|---|
| `PASS` | 자동 연결됨. 그래프를 확인한 뒤 분석에 사용 |
| `REVIEW` | 시험 분석에는 연결되지만 논문용 사용 전 경고 점검 필요 |
| `FAIL` | 감사용 파일만 저장되고 전체 필드 자동 적용은 차단됨 |

보정파일 저장 위치:

```text
output/calibration/<패널영상명>_weighted_dark_calibration.npz
```

QC가 허용하면 현재 세션에 즉시 연결됩니다. 다음 실행부터는 원본 폴더와
`output/calibration/`에서 호환 파일을 자동 탐색하며, 사이드바에서 직접 지정할
수도 있습니다. 작물 영상을 로드한 후 초록색 보정 배너와 다음 CSV 값을 확인합니다.

```text
value_units = reflectance
calibration_applied = true
calibration_qc_status = PASS
normalization_mode = none
```

## 5. 저장 결과 이해하고 연구팀에 전달하기

파일 하나의 대표 결과 폴더:

```text
output/<원본파일명>/
|-- report_<시간>_<방법>.html
|-- spectra_<방법>.csv
|-- spectra_<방법>_reflectance.csv
|-- spectra_<방법>_raw_dn.csv
|-- spectra_<방법>_processed.csv
|-- spectral_samples.h5
|-- processing_manifest.json
|-- report_config.json
|-- rgb.png
|-- ndvi.png
|-- class_map_<방법>.png
|-- cluster_map.png
|-- cluster_overlay.png
`-- cluster_review.npz
```

| 결과 | 용도 |
|---|---|
| `report_*.html` | 연구팀이 가장 쉽게 보는 이미지·스펙트럼·QC 리포트 |
| `spectra_*_reflectance.csv` | PASS 보정 확인 후 과학 분석에 사용하는 반사율 |
| `spectra_*_raw_dn.csv` | 촬영 및 보정 전후 문제 진단 |
| `spectra_*_processed.csv` | 보정이 없을 때의 상대/정규화 값. 절대 반사율이 아님 |
| `spectral_samples.h5` | 클러스터별 실제 픽셀 스펙트럼, 좌표, 클래스, 가중치와 raw DN을 압축 저장한 모델 개발용 파일 |
| `processing_manifest.json` | 원본, 보정, 정규화, 방법과 설정의 재현 기록 |
| `report_config.json` | 이번 리포트에서 선택한 이미지·통계·지수 항목 |
| 클러스터맵·오버레이 | 공간적으로 클러스터링이 타당한지 확인 |
| `daily_report_*.html`, `daily_summary_*.csv` | 한 번의 배치에 포함된 모든 파일 비교 |
| `Field_Results.xlsx` | 팀/측정일 대시보드, 필드 요약, 스펙트럼과 경고 |

왼쪽의 **모델 학습용 실제 스펙트럼**에서 저장 여부와 클러스터별 최대 개수를
정합니다. 기본값은 각 최종 클러스터에서 1,000개를 고정 난수 시드로 비복원
표본추출하는 것입니다. 같은 입력과 설정이면 같은 픽셀이 선택됩니다. Hybrid를
사용하면 최종 클러스터뿐 아니라 원래의 sunlit/shadow/soil 구분도 함께 저장됩니다.

중요: 이 파일의 1,000개 픽셀은 1,000개의 독립적인 수확량/Vcmax 표본이 아닙니다.
모두 한 영상(한 플랏)에 속하는 반복 관측이므로 모델에서는 **파일/plot_id별 하나의
스펙트럼 집합**으로 묶고, 학습·검증·시험 분할도 플랏 단위로 해야 합니다.

연구팀에는 다음 자료를 우선 전달하는 것이 간단합니다.

1. 파일별 또는 팀별 HTML 리포트
2. 일일 배치인 경우 `Field_Results.xlsx`
3. 후속 통계에 필요한 보정 반사율 CSV
4. 재현성 기록인 `processing_manifest.json`

논문용 결과를 전달하기 전 확인:

- 보정 QC가 `PASS`인지
- 리포트 단위가 Raw DN이 아니라 Reflectance인지
- 요청한 식생지수가 실제로 계산되었는지
- 클러스터 오버레이가 의미 있는 영역과 일치하는지
- 설명되지 않는 파장 점프나 포화 경고가 없는지

## 자주 생기는 문제

| 증상 | 확인할 것 |
|---|---|
| ENVI/BIL이 열리지 않음 | 바이너리와 `.hdr`를 같은 폴더에 두고 헤더 선택 |
| CERES 분석을 시작할 수 없음 | 내부 항목 선택 후 **선택 항목 분석 준비** 실행 |
| 반사율 보정 배너가 없음 | 보정파일 없음, 파장/센서 불일치 또는 QC FAIL 확인 |
| NDVI가 계산되지 않음 | 보정된 red와 NIR 파장이 모두 있는지 확인 |
| 특정 파장 구간이 계단처럼 뜀 | 패널 가중치, 포화, 센서 경계와 보정 QC 확인 |
| 분석이 너무 오래 걸림 | 다운샘플 4, 단일 파일 시험 또는 **실행 중지** 사용 |

알고리즘 원리, CSV 전체 열과 상세 문제 해결은
[전체 한글 설명서](manual_ko.html) 또는 [USAGE_KO.md](USAGE_KO.md)를 참고하세요.
